/* sync tasks you have to wait for it to finish before moving on to the next task
meanwhile for async tasks you can move onto another task before it finishes and then
come back to the async until it finishes*/

//*********************************************************

/* TIMER METHODS */
// setTimeout() takes two things a func() and a time -only happens once
// setInterval() it happens every time for example it happens every 3 seconds